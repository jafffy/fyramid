#include <fyramid.h>

#include "fyramid.h"

namespace fy {

File* open(const char* path, OpenMode openMode) {
    return nullptr;
}

void close(File* fp) {
}

void writeBulk(const void* data, size_t nbyte, off_t offset) {
}

}
